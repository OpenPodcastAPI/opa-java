services:
  postgres:
    image: 'postgres:latest'
    env_file:
      - .env
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: 'redis:latest'
    env_file:
      - .env
    ports:
      - '6379:6379'

volumes:
  postgres_data: